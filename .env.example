# Twinly Content server-to-server resource connector.
# Keep the key server-only: never use a VITE_ prefix.
TWINLY_CONTENT_API_URL=http://localhost:8081
TWINLY_CONTENT_AGENT_API_KEY=
RSP_ENCRYPTION_KEY=
RSP_ENCRYPTION_KEY_VERSION=v1
